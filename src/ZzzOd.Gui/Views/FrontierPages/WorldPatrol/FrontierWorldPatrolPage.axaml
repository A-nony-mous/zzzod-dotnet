<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:fa="using:FluentAvalonia.UI.Controls"
             xmlns:local="using:ZzzOd.Gui.Views.FrontierPages.WorldPatrol"
             xmlns:model="using:ZzzOd.Gui.PageModels.ApplicationSettings"
             xmlns:controls="using:ZzzOd.Gui.Controls"
             x:Class="ZzzOd.Gui.Views.FrontierPages.WorldPatrol.FrontierWorldPatrolPage">
  <Grid>
    <TabControl Classes="zzz-pivot" x:Name="SettingsTabView"
                SelectionChanged="OnSettingsTabSelectionChanged">
      <TabItem Classes="zzz-pivot-item" Header="锄大地配置">
        <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
          <StackPanel Margin="{DynamicResource SampleAppPageMargin}"
                      Spacing="{DynamicResource SampleAppSectionSpacing}">
            <fa:FAInfoBar x:Name="SettingsErrorBar" IsOpen="False" IsClosable="True" Severity="Error" />
            <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
              <StackPanel Grid.Column="0" Spacing="1">
                <fa:FASettingsExpander Header="使用说明">
                  <fa:FASettingsExpander.Footer>
                    <Button Content="查看" Click="OnHelpClicked" />
                  </fa:FASettingsExpander.Footer>
                </fa:FASettingsExpander>
                <fa:FASettingsExpander Header="自动战斗">
                  <fa:FASettingsExpander.Footer><fa:FAComboBox x:Name="AutoBattleCombo" Tag="auto_battle" Width="210" SelectionChanged="OnConfigComboChanged" /></fa:FASettingsExpander.Footer>
                </fa:FASettingsExpander>
                <fa:FASettingsExpander Header="界面消失预警时间" Description="UI完全消失持续到该秒数后判定为卡电梯">
                  <fa:FASettingsExpander.Footer><fa:FANumberBox x:Name="UiDisappearSecondsBox" Tag="ui_disappear_seconds" Minimum="0" Maximum="999" SmallChange="1" ValueChanged="OnConfigNumberChanged" /></fa:FASettingsExpander.Footer>
                </fa:FASettingsExpander>
                <fa:FASettingsExpander Header="单条路线重试上限" Description="单条路线累计重试次数，超限后跳过该路线">
                  <fa:FASettingsExpander.Footer><fa:FANumberBox x:Name="RouteRetryTimesBox" Tag="route_retry_times" Minimum="0" Maximum="99" SmallChange="1" ValueChanged="OnConfigNumberChanged" /></fa:FASettingsExpander.Footer>
                </fa:FASettingsExpander>
                <fa:FASettingsExpander Header="锄地每日循环次数" Description="一次任务执行内总共执行的轮数">
                  <fa:FASettingsExpander.Footer><fa:FANumberBox x:Name="DailyLoopCountBox" Tag="daily_loop_count" Minimum="0" Maximum="99" SmallChange="1" ValueChanged="OnConfigNumberChanged" /></fa:FASettingsExpander.Footer>
                </fa:FASettingsExpander>
              </StackPanel>
              <StackPanel Grid.Column="1" Spacing="1">
                <fa:FASettingsExpander x:Name="RunRecordItem" Header="运行记录">
                  <fa:FASettingsExpander.Footer><Button Content="重置记录" Click="OnResetRecordClicked" /></fa:FASettingsExpander.Footer>
                </fa:FASettingsExpander>
                <fa:FASettingsExpander Header="路线名单">
                  <fa:FASettingsExpander.Footer><fa:FAComboBox x:Name="RouteListConfigCombo" Tag="route_list" Width="210" SelectionChanged="OnConfigComboChanged" /></fa:FASettingsExpander.Footer>
                </fa:FASettingsExpander>
                <fa:FASettingsExpander Header="界面消失处理方式" Description="判定为疑似卡电梯后的处理方式">
                  <fa:FASettingsExpander.Footer><fa:FAComboBox x:Name="UiDisappearActionCombo" Tag="ui_disappear_action" Width="210" SelectionChanged="OnConfigComboChanged" /></fa:FASettingsExpander.Footer>
                </fa:FASettingsExpander>
                <fa:FASettingsExpander Header="路线重试处理方式" Description="路线脱困失败，重试后再次卡住时的处理方式">
                  <fa:FASettingsExpander.Footer><fa:FAComboBox x:Name="RouteRetryActionCombo" Tag="route_retry_action" Width="210" SelectionChanged="OnConfigComboChanged" /></fa:FASettingsExpander.Footer>
                </fa:FASettingsExpander>
                <fa:FASettingsExpander Header="每轮最少占用时长（敌人刷新时间）" Description="每轮跑完：不足则补等到该秒数，超过则立即开始下一轮">
                  <fa:FASettingsExpander.Footer><fa:FANumberBox x:Name="LoopIntervalSecondsBox" Tag="loop_interval_seconds" Minimum="0" Maximum="86400" SmallChange="1" ValueChanged="OnConfigNumberChanged" /></fa:FASettingsExpander.Footer>
                </fa:FASettingsExpander>
              </StackPanel>
            </Grid>
          </StackPanel>
        </ScrollViewer>
      </TabItem>

      <TabItem Classes="zzz-pivot-item" Header="路线列表">
        <Grid Margin="{DynamicResource SampleAppPageMargin}" ColumnDefinitions="360,*" ColumnSpacing="12">
          <StackPanel Grid.Column="0" Spacing="8">
            <fa:FAInfoBar x:Name="RouteListErrorBar" IsOpen="False" IsClosable="True" Severity="Error" />
            <fa:FASettingsExpander Header="名单选择">
              <fa:FASettingsExpander.Footer>
                <StackPanel Orientation="Horizontal" Spacing="8">
                  <fa:FAComboBox x:Name="EditorRouteListCombo" Width="180" SelectionChanged="OnEditorRouteListChanged" />
                  <fa:FAComboBox x:Name="ListTypeCombo" Width="110" SelectionChanged="OnListTypeChanged" />
                </StackPanel>
              </fa:FASettingsExpander.Footer>
            </fa:FASettingsExpander>
            <fa:FACommandBar>
              <fa:FACommandBar.PrimaryCommands>
                <fa:FACommandBarButton x:Name="NewListButton" Label="新建" Click="OnNewListClicked" />
                <fa:FACommandBarButton x:Name="SaveListButton" Label="保存" Click="OnSaveListClicked" />
                <fa:FACommandBarButton x:Name="DeleteListButton" Label="删除" Click="OnDeleteListClicked" />
                <fa:FACommandBarButton x:Name="CancelListButton" Label="取消" Click="OnCancelClicked" />
              </fa:FACommandBar.PrimaryCommands>
            </fa:FACommandBar>
            <fa:FASettingsExpander Header="区域选择">
              <fa:FASettingsExpander.Footer>
                <StackPanel Orientation="Horizontal" Spacing="8">
                  <fa:FAComboBox x:Name="EntryCombo" Width="150" SelectionChanged="OnEntryChanged" />
                  <fa:FAComboBox x:Name="AreaCombo" Width="180" SelectionChanged="OnAreaChanged" />
                </StackPanel>
              </fa:FASettingsExpander.Footer>
            </fa:FASettingsExpander>
            <fa:FACommandBar>
              <fa:FACommandBar.PrimaryCommands>
                <fa:FACommandBarButton x:Name="AddAreaButton" Label="添加整个区域" Click="OnAddAreaClicked" />
                <fa:FACommandBarButton x:Name="AddRouteButton" Label="添加单条路线" Click="OnAddRouteClicked" />
              </fa:FACommandBar.PrimaryCommands>
            </fa:FACommandBar>
            <fa:FACommandBar>
              <fa:FACommandBar.PrimaryCommands>
                <fa:FACommandBarButton x:Name="MoveUpButton" Label="上移" Click="OnMoveUpClicked" />
                <fa:FACommandBarButton x:Name="MoveDownButton" Label="下移" Click="OnMoveDownClicked" />
                <fa:FACommandBarButton x:Name="RemoveButton" Label="移除" Click="OnRemoveClicked" />
              </fa:FACommandBar.PrimaryCommands>
            </fa:FACommandBar>
          </StackPanel>
          <Grid Grid.Column="1" RowDefinitions="Auto,*,Auto,*" RowSpacing="8">
            <TextBlock Grid.Row="0" FontSize="{DynamicResource ZzzFontSizeBodyLarge}" FontWeight="SemiBold" Text="路线列表" />
            <ListBox x:Name="ChosenRouteList" Grid.Row="1" SelectionChanged="OnRouteSelectionChanged" />
            <TextBlock Grid.Row="2" FontSize="{DynamicResource ZzzFontSizeBody}" FontWeight="SemiBold" Text="当前区域可用路线" />
            <ListBox x:Name="AvailableRouteList" Grid.Row="3" SelectionChanged="OnAvailableRouteSelectionChanged" />
          </Grid>
        </Grid>
      </TabItem>

      <TabItem Classes="zzz-pivot-item" Header="大地图录制">
        <Grid Margin="{DynamicResource SampleAppPageMargin}" ColumnDefinitions="360,220,*" ColumnSpacing="12">
          <ScrollViewer Grid.Column="0"
                        HorizontalScrollBarVisibility="Disabled"
                        VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="8">
              <fa:FAInfoBar x:Name="LargeMapErrorBar"
                          IsOpen="False"
                          IsClosable="True"
                          Severity="Error" />
              <fa:FASettingsExpander Header="区域">
                <fa:FASettingsExpander.Footer>
                  <StackPanel Orientation="Horizontal" Spacing="8">
                    <fa:FAComboBox x:Name="LargeMapEntryCombo"
                                   Width="145"
                                   SelectionChanged="OnLargeMapEntryChanged" />
                    <fa:FAComboBox x:Name="LargeMapAreaCombo"
                                   Width="175"
                                   SelectionChanged="OnLargeMapAreaChanged" />
                  </StackPanel>
                </fa:FASettingsExpander.Footer>
              </fa:FASettingsExpander>
              <fa:FACommandBar>
                <fa:FACommandBar.PrimaryCommands>
                  <fa:FACommandBarButton x:Name="LoadLargeMapButton"
                                       Label="加载"
                                       Click="OnLoadLargeMapClicked" />
                  <fa:FACommandBarButton x:Name="SaveLargeMapButton"
                                       Label="保存"
                                       Click="OnSaveLargeMapClicked" />
                  <fa:FACommandBarButton x:Name="DeleteLargeMapButton"
                                       Label="删除"
                                       Click="OnDeleteLargeMapClicked" />
                  <fa:FACommandBarButton x:Name="CancelLargeMapButton"
                                       Label="取消"
                                       Click="OnCancelLargeMapClicked" />
                </fa:FACommandBar.PrimaryCommands>
              </fa:FACommandBar>
              <fa:FACommandBar>
                <fa:FACommandBar.PrimaryCommands>
                  <fa:FACommandBarButton x:Name="CaptureLargeMapButton"
                                       Label="截图(1)"
                                       Click="OnCaptureLargeMapClicked" />
                  <fa:FACommandBarButton x:Name="CalculateLargeMapPositionButton"
                                       Label="定位(2)"
                                       Click="OnCalculateLargeMapPositionClicked" />
                  <fa:FACommandBarButton x:Name="ToggleLargeMapOverlapButton"
                                       Label="重叠(3)"
                                       Click="OnToggleLargeMapOverlapClicked" />
                  <fa:FACommandBarButton x:Name="MergeLargeMapButton"
                                       Label="合并(4)"
                                       Click="OnMergeLargeMapClicked" />
                  <fa:FACommandBarButton x:Name="UndoLargeMapButton"
                                       Label="回退"
                                       Click="OnUndoLargeMapClicked" />
                </fa:FACommandBar.PrimaryCommands>
              </fa:FACommandBar>
              <fa:FACommandBar>
                <fa:FACommandBar.PrimaryCommands>
                  <fa:FACommandBarButton x:Name="EditLargeMapIconsButton"
                                       Label="编辑图标"
                                       Click="OnEditLargeMapIconsClicked" />
                </fa:FACommandBar.PrimaryCommands>
              </fa:FACommandBar>
              <fa:FASettingsExpander Header="使用图标计算坐标">
                <fa:FASettingsExpander.Footer>
                  <ToggleSwitch x:Name="UseLargeMapIconsSwitch" />
                </fa:FASettingsExpander.Footer>
              </fa:FASettingsExpander>
              <fa:FASettingsExpander Header="图标匹配阈值"
                                       Description="调整图标识别的匹配阈值，默认0.7">
                <fa:FASettingsExpander.Footer>
                  <StackPanel Orientation="Horizontal" Spacing="8">
                    <fa:FANumberBox x:Name="LargeMapIconThresholdBox"
                                  Width="140"
                                  Minimum="0.1"
                                  Maximum="1.0"
                                  SmallChange="0.1"
                                  Value="0.7" />
                    <Button Content="应用" Click="OnApplyLargeMapIconThresholdClicked" />
                  </StackPanel>
                </fa:FASettingsExpander.Footer>
              </fa:FASettingsExpander>
              <fa:FASettingsExpander Header="缩放"
                                       Description="调整大地图的，只有第一次需要">
                <fa:FASettingsExpander.Footer>
                  <StackPanel Orientation="Horizontal" Spacing="8">
                    <fa:FANumberBox x:Name="LargeMapScaleBox"
                                  Width="140"
                                  Minimum="0"
                                  Maximum="99"
                                  SmallChange="1"
                                  Value="40" />
                    <Button Content="应用" Click="OnApplyLargeMapScaleClicked" />
                  </StackPanel>
                </fa:FASettingsExpander.Footer>
              </fa:FASettingsExpander>
              <fa:FASettingsExpander Header="角色坐标"
                                       Description="点击大地图可更新坐标">
                <fa:FASettingsExpander.Footer>
                  <StackPanel Spacing="8">
                    <TextBlock x:Name="LargeMapPositionText" />
                    <StackPanel Orientation="Horizontal" Spacing="8">
                      <fa:FANumberBox x:Name="LargeMapHorizontalMoveBox"
                                    Width="115"
                                    Minimum="-9999"
                                    Maximum="9999"
                                    SmallChange="1" />
                      <Button Content="横移" Click="OnMoveLargeMapHorizontalClicked" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                      <fa:FANumberBox x:Name="LargeMapVerticalMoveBox"
                                    Width="115"
                                    Minimum="-9999"
                                    Maximum="9999"
                                    SmallChange="1" />
                      <Button Content="纵移" Click="OnMoveLargeMapVerticalClicked" />
                    </StackPanel>
                  </StackPanel>
                </fa:FASettingsExpander.Footer>
              </fa:FASettingsExpander>
              <fa:FAInfoBar x:Name="LargeMapStatusBar"
                          IsOpen="False"
                          IsClosable="False"
                          Severity="Informational"
                          Title="大地图录制" />
              <ContentControl x:Name="LargeMapLogHost"
                              MinHeight="120"
                              MaxHeight="160" />
            </StackPanel>
          </ScrollViewer>

          <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="12">
              <Image x:Name="LargeMapMiniMap1Image"
                     MinHeight="180"
                     Stretch="Uniform" />
              <Image x:Name="LargeMapMiniMap2Image"
                     MinHeight="180"
                     Stretch="Uniform" />
              <Image x:Name="LargeMapMiniMapMergedImage"
                     MinHeight="180"
                     Stretch="Uniform" />
            </StackPanel>
          </ScrollViewer>

          <local:FrontierWorldPatrolImageViewer x:Name="LargeMapViewer"
                                          Grid.Column="2"
                                          PointClicked="OnLargeMapViewerPointClicked" />
        </Grid>
      </TabItem>

      <TabItem Classes="zzz-pivot-item" Header="锄地路线录制">
        <Grid Margin="{DynamicResource SampleAppPageMargin}" ColumnDefinitions="380,220,*" ColumnSpacing="12">
          <ScrollViewer Grid.Column="0" HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="8">
              <fa:FAInfoBar x:Name="RouteEditorErrorBar" IsOpen="False" IsClosable="True" Severity="Error" />
              <fa:FASettingsExpander Header="路线">
                <fa:FASettingsExpander.Footer>
                  <StackPanel Spacing="8">
                    <fa:FAComboBox x:Name="RouteEntryCombo" Width="330" SelectionChanged="OnRouteEntryChanged" />
                    <fa:FAComboBox x:Name="RouteAreaCombo" Width="330" SelectionChanged="OnRouteAreaChanged" />
                    <fa:FAComboBox x:Name="RouteCombo" Width="330" SelectionChanged="OnRouteChanged" />
                  </StackPanel>
                </fa:FASettingsExpander.Footer>
              </fa:FASettingsExpander>
              <fa:FACommandBar>
                <fa:FACommandBar.PrimaryCommands>
                  <fa:FACommandBarButton x:Name="NewRouteButton" Label="新增" Click="OnNewRouteClicked" />
                  <fa:FACommandBarButton x:Name="SaveRouteButton" Label="保存" Click="OnSaveRouteClicked" />
                  <fa:FACommandBarButton x:Name="DeleteRouteButton" Label="删除" Click="OnDeleteRouteClicked" />
                  <fa:FACommandBarButton x:Name="CancelRouteButton" Label="取消" Click="OnCancelRouteClicked" />
                  <fa:FACommandBarButton x:Name="EditOperationsButton" Label="编辑操作" Click="OnEditOperationsClicked" />
                </fa:FACommandBar.PrimaryCommands>
              </fa:FACommandBar>
              <fa:FASettingsExpander Header="传送点" Description="选择路线起始传送点">
                <fa:FASettingsExpander.Footer>
                  <fa:FAComboBox x:Name="TransportPointCombo" Width="220" SelectionChanged="OnRouteEditorSelectionChanged" />
                </fa:FASettingsExpander.Footer>
              </fa:FASettingsExpander>
              <fa:FASettingsExpander Header="操作">
                <fa:FASettingsExpander.Footer>
                  <StackPanel Orientation="Horizontal" Spacing="8">
                    <Button x:Name="CapturePositionButton" Content="截图(1)" Click="OnCapturePositionClicked" />
                    <Button x:Name="AddMoveButton" Content="添加移动(4)" Click="OnAddMoveClicked" />
                    <Button x:Name="UndoMoveButton" Content="回退(5)" Click="OnUndoMoveClicked" />
                  </StackPanel>
                </fa:FASettingsExpander.Footer>
              </fa:FASettingsExpander>
              <fa:FASettingsExpander Header="调试" Description="可选择从第几步开始, 0代表需要传送，1代表已完成第1步">
                <fa:FASettingsExpander.Footer>
                  <StackPanel Orientation="Horizontal" Spacing="8">
                    <fa:FANumberBox x:Name="DebugStartBox" Minimum="0" SmallChange="1" Width="100" />
                    <Button x:Name="DebugRouteButton" Content="调试" Click="OnDebugRouteClicked" />
                  </StackPanel>
                </fa:FASettingsExpander.Footer>
              </fa:FASettingsExpander>
              <fa:FASettingsExpander Header="点击后自动追加移动">
                <fa:FASettingsExpander.Footer>
                  <ToggleSwitch x:Name="AutoAddRouteClickSwitch" IsChecked="True" />
                </fa:FASettingsExpander.Footer>
              </fa:FASettingsExpander>
              <ContentControl x:Name="RouteRecorderLogHost"
                              MinHeight="120"
                              MaxHeight="160" />
            </StackPanel>
          </ScrollViewer>

          <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
            <Image x:Name="RouteMiniMapRoadImage" MinHeight="220" Stretch="Uniform" />
          </ScrollViewer>

          <local:FrontierWorldPatrolImageViewer x:Name="RouteMapViewer"
                                          Grid.Column="2"
                                          PointClicked="OnRouteMapViewerPointClicked" />
        </Grid>
      </TabItem>
    </TabControl>

    <fa:FAContentDialog x:Name="RouteOperationDialog"
                      Title="编辑路线操作"
                      PrimaryButtonText="保存"
                      CloseButtonText="取消"
                      PrimaryButtonClick="OnSaveRouteOperationsClicked">
      <Grid RowDefinitions="Auto,*,Auto" RowSpacing="8" MinWidth="720" MinHeight="420">
        <fa:FAInfoBar x:Name="RouteOperationErrorBar"
                    IsOpen="False"
                    IsClosable="True"
                    Severity="Error"
                    Title="数据错误" />
        <ListBox x:Name="RouteOperationList"
                 Grid.Row="1"
                 SelectionChanged="OnRouteEditorSelectionChanged">
          <ListBox.ItemTemplate>
            <DataTemplate x:DataType="model:ZzzWorldPatrolEditableOperation">
              <Grid ColumnDefinitions="50,120,*,*" ColumnSpacing="8" Margin="0,2">
                <TextBlock Grid.Column="0" Text="{Binding Index}" VerticalAlignment="Center" />
                <TextBlock Grid.Column="1" Text="{Binding OpType}" VerticalAlignment="Center" />
                <TextBox Grid.Column="2" Text="{Binding Data1}" PlaceholderText="数据1" />
                <TextBox Grid.Column="3" Text="{Binding Data2}" PlaceholderText="数据2" />
              </Grid>
            </DataTemplate>
          </ListBox.ItemTemplate>
        </ListBox>
        <fa:FACommandBar Grid.Row="2">
          <fa:FACommandBar.PrimaryCommands>
            <fa:FACommandBarButton x:Name="AddOperationButton" Label="添加操作" Click="OnAddOperationClicked" />
            <fa:FACommandBarButton x:Name="DeleteOperationButton" Label="删除选中" Click="OnDeleteOperationClicked" />
            <fa:FACommandBarButton x:Name="MoveOperationUpButton" Label="上移" Click="OnMoveOperationUpClicked" />
            <fa:FACommandBarButton x:Name="MoveOperationDownButton" Label="下移" Click="OnMoveOperationDownClicked" />
          </fa:FACommandBar.PrimaryCommands>
        </fa:FACommandBar>
      </Grid>
    </fa:FAContentDialog>

    <fa:FAContentDialog x:Name="DeleteRouteOperationDialog"
                      Title="确认删除"
                      PrimaryButtonText="删除"
                      CloseButtonText="取消" />

  </Grid>
</UserControl>
