<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:fa="using:FluentAvalonia.UI.Controls"
             xmlns:local="using:ZzzOd.Gui.Views.FrontierPages.DevTools"
             x:Class="ZzzOd.Gui.Views.FrontierPages.DevTools.FrontierTemplateHelperPage"
             Focusable="True">
  <Grid Margin="{DynamicResource SampleAppPageMargin}"
        ColumnDefinitions="420,172,*"
        ColumnSpacing="8">
    <Grid Grid.Column="0"
          RowDefinitions="Auto,Auto,Auto,*"
          RowSpacing="8">
        <fa:FAInfoBar x:Name="StatusBar"
                    Grid.Row="0"
                    IsClosable="True"
                    IsOpen="False" />

        <fa:FACommandBar Grid.Row="1"
                         DefaultLabelPosition="Right">
          <fa:FACommandBar.Content>
            <fa:FAComboBox x:Name="ExistingTemplateCombo"
                           Width="180"
                           PlaceholderText="选择已有"
                           SelectionChanged="OnExistingTemplateChanged" />
          </fa:FACommandBar.Content>
          <fa:FACommandBar.PrimaryCommands>
            <fa:FACommandBarButton x:Name="CreateButton"
                                 Label="新建"
                                 Click="OnCreateClicked">
              <fa:FACommandBarButton.IconSource><fa:FASymbolIconSource Symbol="Add" /></fa:FACommandBarButton.IconSource>
            </fa:FACommandBarButton>
            <fa:FACommandBarButton x:Name="CopyButton"
                                 Label="复制"
                                 Click="OnCopyClicked">
              <fa:FACommandBarButton.IconSource><fa:FASymbolIconSource Symbol="Copy" /></fa:FACommandBarButton.IconSource>
            </fa:FACommandBarButton>
            <fa:FACommandBarButton x:Name="DeleteButton"
                                 Label="删除"
                                 Click="OnDeleteClicked">
              <fa:FACommandBarButton.IconSource><fa:FASymbolIconSource Symbol="Delete" /></fa:FACommandBarButton.IconSource>
            </fa:FACommandBarButton>
            <fa:FACommandBarButton x:Name="CancelButton"
                                 Label="取消"
                                 Click="OnCancelClicked">
              <fa:FACommandBarButton.IconSource><fa:FASymbolIconSource Symbol="Cancel" /></fa:FACommandBarButton.IconSource>
            </fa:FACommandBarButton>
          </fa:FACommandBar.PrimaryCommands>
        </fa:FACommandBar>

        <fa:FACommandBar Grid.Row="2"
                         DefaultLabelPosition="Right">
          <fa:FACommandBar.PrimaryCommands>
            <fa:FACommandBarButton x:Name="ChooseImageButton"
                                 Label="选择图片"
                                 Click="OnChooseImageClicked">
              <fa:FACommandBarButton.IconSource><fa:FASymbolIconSource Symbol="OpenFile" /></fa:FACommandBarButton.IconSource>
            </fa:FACommandBarButton>
            <fa:FACommandBarButton x:Name="ScreenshotButton"
                                 Label="截图"
                                 Click="OnScreenshotClicked">
              <fa:FACommandBarButton.IconSource><fa:FASymbolIconSource Symbol="Camera" /></fa:FACommandBarButton.IconSource>
            </fa:FACommandBarButton>
            <fa:FACommandBarButton x:Name="SaveConfigButton"
                                 Label="保存配置"
                                 Click="OnSaveConfigClicked">
              <fa:FACommandBarButton.IconSource><fa:FASymbolIconSource Symbol="Save" /></fa:FACommandBarButton.IconSource>
            </fa:FACommandBarButton>
            <fa:FACommandBarButton x:Name="SaveRawButton"
                                 Label="保存模板"
                                 Click="OnSaveRawClicked">
              <fa:FACommandBarButton.IconSource><fa:FASymbolIconSource Symbol="Save" /></fa:FACommandBarButton.IconSource>
            </fa:FACommandBarButton>
            <fa:FACommandBarButton x:Name="SaveMaskButton"
                                 Label="保存掩码"
                                 Click="OnSaveMaskClicked">
              <fa:FACommandBarButton.IconSource><fa:FASymbolIconSource Symbol="Save" /></fa:FACommandBarButton.IconSource>
            </fa:FACommandBarButton>
          </fa:FACommandBar.PrimaryCommands>
        </fa:FACommandBar>

      <ScrollViewer Grid.Row="3"
                    HorizontalScrollBarVisibility="Disabled"
                    VerticalScrollBarVisibility="Auto">
        <StackPanel Spacing="8">
        <fa:FASettingsExpander Header="模板信息"
                               IsExpanded="True">
        <fa:FASettingsExpanderItem Content="画面">
          <fa:FASettingsExpanderItem.IconSource><fa:FASymbolIconSource Symbol="Home" /></fa:FASettingsExpanderItem.IconSource>
          <fa:FASettingsExpanderItem.Footer>
            <TextBox x:Name="TemplateSubDirBox" Width="240" TextChanged="OnTemplateInfoChanged" />
          </fa:FASettingsExpanderItem.Footer>
        </fa:FASettingsExpanderItem>

        <fa:FASettingsExpanderItem Content="模板ID">
          <fa:FASettingsExpanderItem.IconSource><fa:FASymbolIconSource Symbol="Home" /></fa:FASettingsExpanderItem.IconSource>
          <fa:FASettingsExpanderItem.Footer>
            <TextBox x:Name="TemplateIdBox" Width="240" TextChanged="OnTemplateInfoChanged" />
          </fa:FASettingsExpanderItem.Footer>
        </fa:FASettingsExpanderItem>

        <fa:FASettingsExpanderItem Content="模板名称">
          <fa:FASettingsExpanderItem.IconSource><fa:FASymbolIconSource Symbol="Home" /></fa:FASettingsExpanderItem.IconSource>
          <fa:FASettingsExpanderItem.Footer>
            <TextBox x:Name="TemplateNameBox" Width="240" TextChanged="OnTemplateInfoChanged" />
          </fa:FASettingsExpanderItem.Footer>
        </fa:FASettingsExpanderItem>

        <fa:FASettingsExpanderItem Content="微调">
          <fa:FASettingsExpanderItem.IconSource><fa:FASymbolIconSource Symbol="Home" /></fa:FASettingsExpanderItem.IconSource>
          <fa:FASettingsExpanderItem.Footer>
            <StackPanel Orientation="Horizontal" Spacing="6">
              <TextBox x:Name="HorizontalMoveBox" Width="72" PlaceholderText="横" />
              <TextBox x:Name="VerticalMoveBox" Width="72" PlaceholderText="纵" />
              <Button Content="移动" Click="OnMoveClicked" />
            </StackPanel>
          </fa:FASettingsExpanderItem.Footer>
        </fa:FASettingsExpanderItem>

        <fa:FASettingsExpanderItem Content="形状">
          <fa:FASettingsExpanderItem.IconSource><fa:FASymbolIconSource Symbol="Home" /></fa:FASettingsExpanderItem.IconSource>
          <fa:FASettingsExpanderItem.Footer>
            <StackPanel Orientation="Horizontal" Spacing="6">
              <fa:FAComboBox x:Name="ShapeCombo" Width="150" SelectionChanged="OnShapeChanged" />
              <Button x:Name="ShapeHelpButton" ToolTip.Tip="点击查看形状使用说明" Click="OnShapeHelpClicked">
                <fa:FASymbolIcon Symbol="Help" />
              </Button>
            </StackPanel>
          </fa:FASettingsExpanderItem.Footer>
        </fa:FASettingsExpanderItem>

        <fa:FASettingsExpanderItem Content="自动生成掩码">
          <fa:FASettingsExpanderItem.IconSource><fa:FASymbolIconSource Symbol="Home" /></fa:FASettingsExpanderItem.IconSource>
          <fa:FASettingsExpanderItem.Footer>
            <ToggleSwitch x:Name="AutoMaskToggle" Click="OnAutoMaskChanged" />
          </fa:FASettingsExpanderItem.Footer>
        </fa:FASettingsExpanderItem>
        </fa:FASettingsExpander>

        <Grid ColumnDefinitions="42,*" Margin="8,0,8,0">
          <TextBlock Grid.Column="0" Text="操作" FontWeight="SemiBold" />
          <TextBlock Grid.Column="1" Text="点位" FontWeight="SemiBold" />
        </Grid>
        <ItemsControl x:Name="PointList">
          <ItemsControl.ItemTemplate>
            <DataTemplate x:DataType="local:TemplatePointRow">
              <Grid ColumnDefinitions="42,*" Margin="4,2">
                <Button Grid.Column="0"
                        Width="32"
                        Height="32"
                        DataContext="{Binding}"
                        Click="OnPointDeleteClicked">
                  <fa:FASymbolIcon Symbol="Delete" />
                </Button>
                <TextBox Grid.Column="1"
                         Text="{Binding Text}"
                         DataContext="{Binding}"
                         LostFocus="OnPointTextLostFocus" />
              </Grid>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
        </StackPanel>
      </ScrollViewer>
    </Grid>

    <ScrollViewer Grid.Column="1"
                  HorizontalScrollBarVisibility="Disabled"
                  VerticalScrollBarVisibility="Auto">
      <StackPanel Spacing="4">
        <TextBlock Text="模板原图" />
        <Image x:Name="RawPreview" Width="140" Height="140" Stretch="Uniform" />
        <TextBlock Text="模板掩码" />
        <Image x:Name="MaskPreview" Width="140" Height="140" Stretch="Uniform" />
        <TextBlock Text="模板抠图" />
        <Image x:Name="MergePreview" Width="140" Height="140" Stretch="Uniform" />
        <TextBlock Text="反向抠图" />
        <Image x:Name="ReversePreview" Width="140" Height="140" Stretch="Uniform" />
      </StackPanel>
    </ScrollViewer>

    <Grid Grid.Column="2" RowDefinitions="Auto,Auto,*" RowSpacing="8">
      <fa:FASettingsExpander Grid.Row="0"
                             Header="鼠标点击坐标"
                             Description="图片左上角为(0, 0)">
        <fa:FASettingsExpander.IconSource><fa:FASymbolIconSource Symbol="Home" /></fa:FASettingsExpander.IconSource>
        <fa:FASettingsExpander.Footer>
          <StackPanel Orientation="Horizontal" Spacing="6">
            <TextBox x:Name="XPositionBox" Width="72" PlaceholderText="横" IsReadOnly="True" />
            <TextBox x:Name="YPositionBox" Width="72" PlaceholderText="纵" IsReadOnly="True" />
            <Button x:Name="ClearPointsButton" Content="清除点位" Click="OnClearPointsClicked" />
          </StackPanel>
        </fa:FASettingsExpander.Footer>
      </fa:FASettingsExpander>

      <fa:FASettingsExpander Grid.Row="1"
                             Header="历史记录"
                             Description="Ctrl+Z 撤回，Ctrl+Shift+Z 恢复">
        <fa:FASettingsExpander.IconSource><fa:FASymbolIconSource Symbol="Home" /></fa:FASettingsExpander.IconSource>
        <fa:FASettingsExpander.Footer>
          <StackPanel Orientation="Horizontal" Spacing="6">
            <Button x:Name="UndoButton" Content="撤回" Click="OnUndoClicked" />
            <Button x:Name="RedoButton" Content="恢复" Click="OnRedoClicked" />
          </StackPanel>
        </fa:FASettingsExpander.Footer>
      </fa:FASettingsExpander>

      <ScrollViewer Grid.Row="2"
                    HorizontalScrollBarVisibility="Auto"
                    VerticalScrollBarVisibility="Auto">
        <Image x:Name="ScreenImage"
               MinWidth="420"
               MinHeight="360"
               Stretch="Uniform"
               PointerPressed="OnScreenPointerPressed"
               PointerMoved="OnScreenPointerMoved"
               PointerReleased="OnScreenPointerReleased" />
      </ScrollViewer>
    </Grid>

    <fa:FATeachingTip x:Name="ShapeTeachingTip"
                    Target="{Binding ElementName=ShapeHelpButton}"
                    Title="形状使用说明"
                    IsLightDismissEnabled="True"
                    PreferredPlacement="Right"
                    TailVisibility="Visible" />
  </Grid>
</UserControl>
