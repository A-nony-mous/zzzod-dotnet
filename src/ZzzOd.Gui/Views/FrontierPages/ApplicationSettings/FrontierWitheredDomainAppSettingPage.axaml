<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:fa="using:FluentAvalonia.UI.Controls"
             x:Class="ZzzOd.Gui.Views.FrontierPages.ApplicationSettings.FrontierWitheredDomainAppSettingPage">
  <Grid>
    <fa:FATabView TabWidthMode="SizeToContent">
      <fa:FATabViewItem Header="枯萎之都配置" IsClosable="False">
        <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
          <StackPanel Margin="{DynamicResource SampleAppPageMargin}"
                      Spacing="{DynamicResource SampleAppSectionSpacing}">
            <fa:FAInfoBar x:Name="BaseErrorBar" IsOpen="False" IsClosable="True" Severity="Error" />
            <Grid ColumnDefinitions="*,6,*">
              <StackPanel Spacing="6">
                <fa:FASettingsExpander Header="使用说明">
                  <fa:FASettingsExpander.Footer>
                    <Button Content="打开说明" Click="OnHelpClicked" />
                  </fa:FASettingsExpander.Footer>
                </fa:FASettingsExpander>
                <fa:FASettingsExpander Header="挑战副本" Description="选择空洞及难度等级">
                  <fa:FASettingsExpander.Footer>
                    <fa:FAComboBox x:Name="MissionCombo" Width="220" SelectionChanged="OnBaseComboChanged" Tag="mission_name" />
                  </fa:FASettingsExpander.Footer>
                </fa:FASettingsExpander>
                <fa:FASettingsExpander Header="每周基础次数" Description="完整通关，用于完成委托任务">
                  <fa:FASettingsExpander.Footer>
                    <fa:FANumberBox x:Name="WeeklyTimesNumber" Minimum="0" Maximum="99" SmallChange="1" ValueChanged="OnBaseNumberChanged" Tag="weekly_plan_times" />
                  </fa:FASettingsExpander.Footer>
                </fa:FASettingsExpander>
                <fa:FASettingsExpander Header="额外刷取" Description="完成基础次数后，继续刷取">
                  <fa:FASettingsExpander.Footer>
                    <fa:FAComboBox x:Name="ExtraTaskCombo" Width="180" SelectionChanged="OnBaseComboChanged" Tag="extra_task" />
                  </fa:FASettingsExpander.Footer>
                </fa:FASettingsExpander>
              </StackPanel>
              <StackPanel Grid.Column="2" Spacing="6">
                <fa:FASettingsExpander x:Name="RunRecordItem" Header="运行记录">
                  <fa:FASettingsExpander.Footer>
                    <Button Content="重置记录" Click="OnResetRecordClicked" />
                  </fa:FASettingsExpander.Footer>
                </fa:FASettingsExpander>
                <fa:FASettingsExpander Header="挑战配置" Description="选择角色、鸣徽和事件">
                  <fa:FASettingsExpander.Footer>
                    <fa:FAComboBox x:Name="BaseChallengeCombo" Width="220" SelectionChanged="OnBaseComboChanged" Tag="challenge_config" />
                  </fa:FASettingsExpander.Footer>
                </fa:FASettingsExpander>
                <fa:FASettingsExpander Header="每天进入次数" Description="将空洞分摊到每天运行">
                  <fa:FASettingsExpander.Footer>
                    <fa:FANumberBox x:Name="DailyTimesNumber" Minimum="0" Maximum="99" SmallChange="1" ValueChanged="OnBaseNumberChanged" Tag="daily_plan_times" />
                  </fa:FASettingsExpander.Footer>
                </fa:FASettingsExpander>
                <fa:FASettingsExpander Header="额外刷取方式" Description="额外刷取时，选择何时退出">
                  <fa:FASettingsExpander.Footer>
                    <fa:FAComboBox x:Name="ExtraExitCombo" Width="180" SelectionChanged="OnBaseComboChanged" Tag="extra_exit" />
                  </fa:FASettingsExpander.Footer>
                </fa:FASettingsExpander>
              </StackPanel>
            </Grid>
          </StackPanel>
        </ScrollViewer>
      </fa:FATabViewItem>

      <fa:FATabViewItem Header="挑战配置-枯" IsClosable="False">
        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
          <StackPanel Margin="{DynamicResource SampleAppPageMargin}"
                      Spacing="{DynamicResource SampleAppSectionSpacing}">
            <fa:FAInfoBar x:Name="ChallengeErrorBar" IsOpen="False" IsClosable="True" Severity="Error" />
            <fa:FACommandBar DefaultLabelPosition="Right">
              <fa:FACommandBar.PrimaryCommands>
                <fa:FACommandBarElementContainer>
                  <fa:FAComboBox x:Name="ExistingChallengeCombo"
                                 Width="220"
                                 PlaceholderText="选择已有"
                                 SelectionChanged="OnExistingChallengeChanged" />
                </fa:FACommandBarElementContainer>
                <fa:FACommandBarButton x:Name="CreateButton" Label="新建" Click="OnCreateClicked" />
                <fa:FACommandBarButton x:Name="CopyButton" Label="复制" Click="OnCopyClicked" />
                <fa:FACommandBarButton x:Name="DeleteButton" Label="删除" Click="OnDeleteClicked" />
                <fa:FACommandBarButton x:Name="CloseButton" Label="关闭" Click="OnCloseEditorClicked" />
              </fa:FACommandBar.PrimaryCommands>
            </fa:FACommandBar>
            <Grid ColumnDefinitions="*,8,*">
              <StackPanel Spacing="6">
                <fa:FASettingsExpander Header="配置名称" Description="默认配置复制后可修改">
                  <fa:FASettingsExpander.Footer>
                    <TextBox x:Name="ChallengeNameText" Width="220" LostFocus="OnChallengeEditorLostFocus" />
                  </fa:FASettingsExpander.Footer>
                </fa:FASettingsExpander>
                <fa:FASettingsExpander Header="目标配队" Description="按照自动战斗配置 选择角色位置">
                  <fa:FASettingsExpander.Footer>
                    <StackPanel Orientation="Horizontal" Spacing="6">
                      <fa:FAComboBox x:Name="Agent1Combo" Width="150" IsEditable="True" LostFocus="OnChallengeEditorLostFocus" />
                      <fa:FAComboBox x:Name="Agent2Combo" Width="150" IsEditable="True" LostFocus="OnChallengeEditorLostFocus" />
                      <fa:FAComboBox x:Name="Agent3Combo" Width="150" IsEditable="True" LostFocus="OnChallengeEditorLostFocus" />
                    </StackPanel>
                  </fa:FASettingsExpander.Footer>
                </fa:FASettingsExpander>
                <fa:FASettingsExpander Header="仅购买优先级" Description="邦布商人购买时只购买符合优先级的">
                  <fa:FASettingsExpander.Footer>
                    <ToggleSwitch x:Name="BuyOnlyPriorityToggle" Click="OnChallengeEditorChanged" />
                  </fa:FASettingsExpander.Footer>
                </fa:FASettingsExpander>
                <fa:FASettingsExpander Header="自动战斗">
                  <fa:FASettingsExpander.Footer>
                    <fa:FAComboBox x:Name="AutoBattleCombo" Width="220" SelectionChanged="OnChallengeEditorChanged" />
                  </fa:FASettingsExpander.Footer>
                </fa:FASettingsExpander>
                <fa:FASettingsExpander Header="寻路方式">
                  <fa:FASettingsExpander.Footer>
                    <fa:FAComboBox x:Name="PathFindingCombo" Width="180" SelectionChanged="OnPathFindingChanged" />
                  </fa:FASettingsExpander.Footer>
                </fa:FASettingsExpander>
                <Grid x:Name="CustomPathGrid" ColumnDefinitions="*,6,*,6,*">
                  <StackPanel>
                    <TextBlock Text="一步可达时前往" FontSize="16" FontWeight="SemiBold" />
                    <TextBox x:Name="GoInOneStepText" AcceptsReturn="True" MinHeight="130" TextWrapping="Wrap" LostFocus="OnChallengeEditorLostFocus" />
                  </StackPanel>
                  <StackPanel Grid.Column="2">
                    <TextBlock Text="优先途经点" FontSize="16" FontWeight="SemiBold" />
                    <TextBox x:Name="WaypointText" AcceptsReturn="True" MinHeight="130" TextWrapping="Wrap" LostFocus="OnChallengeEditorLostFocus" />
                  </StackPanel>
                  <StackPanel Grid.Column="4">
                    <TextBlock Text="避免途经点" FontSize="16" FontWeight="SemiBold" />
                    <TextBox x:Name="AvoidText" AcceptsReturn="True" MinHeight="130" TextWrapping="Wrap" LostFocus="OnChallengeEditorLostFocus" />
                  </StackPanel>
                </Grid>
              </StackPanel>
              <StackPanel Grid.Column="2" Spacing="6">
                <TextBlock Text="奖励优先级" FontSize="24" FontWeight="SemiBold" />
                <TextBox x:Name="ResoniumPriorityText" AcceptsReturn="True" MinHeight="480" TextWrapping="Wrap" LostFocus="OnChallengeEditorLostFocus" />
              </StackPanel>
            </Grid>
          </StackPanel>
        </ScrollViewer>
      </fa:FATabViewItem>
    </fa:FATabView>

    <fa:FAContentDialog x:Name="DeleteDialog"
                      Title="删除确认"
                      PrimaryButtonText="确定"
                      CloseButtonText="取消"
                      DefaultButton="Primary">
      <TextBlock Text="即将删除该配置" />
    </fa:FAContentDialog>
  </Grid>
</UserControl>
